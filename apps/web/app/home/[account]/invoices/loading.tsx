import { WorkspaceTablePageSkeleton } from '~/components/workspace-shell/workspace-page-skeletons';

export default function InvoicesLoading() {
  return <WorkspaceTablePageSkeleton rows={8} columns={5} />;
}
