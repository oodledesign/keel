# TODO Launch

- Set `NEXT_PUBLIC_SITE_INDEXABLE=true` and verify no `noindex` tag renders.
- Verify `sitemap.xml` includes compare pages.
- Verify every public marketing page has a unique meta title, description, and OG image.
- TODO: Wire the coming-soon signup confirmation flow to SES after launch interest capture is validated.
