/** Stored shortcut that points at a concrete app path (nav-driven catalog items). */
export const SHORTCUT_CATALOG_ROUTE = 'app.route';

/** Dynamic entity shortcuts (e.g. a specific Rankly project). */
export const SHORTCUT_CATALOG_RANKLY_PROJECT = 'workspace.rankly.project';
