/** Shared exporter enums — safe for client + server imports. */

export type PromptPackTarget = 'webflow' | 'astro' | 'next' | 'ozer_sites';

/** Scaffold zip packs on the Export tab (Prompt D2). */
export type ScaffoldPackKind = 'webflow' | 'astro' | 'next';
