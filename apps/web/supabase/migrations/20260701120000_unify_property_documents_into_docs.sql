-- Superseded by 20260701140000_finish_unify_property_documents.sql after a failed
-- partial apply left this version in schema_migrations. Kept as a no-op so CLI
-- history stays consistent for environments that never recorded this version.
SELECT 1;
